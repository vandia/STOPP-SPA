<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.urjc.etsii.dlsi.pfc.stopp_spa.UploadImageActivity">

    <ImageView
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:id="@+id/imageUploaded"
        android:background="@color/upload_picture_background"
        android:clickable="true"
        android:onClick="pickPhoto"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="89dp" />

    <ImageView
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:id="@+id/uploadButton"
        android:src="@drawable/upload_image"
        android:background="@android:color/transparent"
        android:layout_alignBottom="@+id/imageUploaded"
        android:layout_alignLeft="@+id/imageUploaded"
        android:onClick="pickPhoto"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="New Button"
        android:id="@+id/button"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:onClick="uploadPhoto"/>

</RelativeLayout>
