<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:gravity="center_horizontal"
    android:orientation="vertical" android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.urjc.etsii.dlsi.pfc.stopp_spa.LoginActivity"
    android:weightSum="1"
    android:id="@+id/login_form">

    <TextView
        android:id="@+id/login_label"
        android:layout_width="fill_parent"
        android:layout_height="50dp"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:text="@string/select_name"
        android:layout_gravity="left"
        android:layout_alignParentTop="true"/>

    <ListView
        android:id="@+id/names_list_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/login_label"
        android:layout_above="@+id/space_login"/>

    <Space
        android:layout_width="40px"
        android:layout_height="40px"
        android:id="@+id/space_login"
        android:layout_above="@+id/new_user_button"/>


    <Button android:id="@+id/new_user_button" style="?android:textAppearanceSmall"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:text="@string/action_new_user"
            android:textStyle="bold"
            android:layout_alignParentBottom="true"/>

</RelativeLayout>



